# Corrected findings register reconciliation

Read-only source/test/commit audit at `5194303`, 2026-09-21, with the coordinator's subsequent F16 closure and workload rerun recorded below. The governing register is `refactor.md` §5, including the Antigravity reconciliation notes. This table identifies implemented behavior and concrete regressions; it does not turn source inspection into executed-test evidence. The coordinator's retained dual-host `0a640b2` suites covered the existing host tests below; later focused tests and simulator runs retain their own revision/results. No Pi or simulator was run by this reviewer.

| Finding | Final behavior / deliberate decision | Representative implementation commits | Concrete regression / evidence boundary |
| --- | --- | --- | --- |
| F01 LAND through failed receive | Bound the telemetry drain, resolve human takeover first, then retry the best-effort LAND obligation despite receive/heartbeat/write failure. | `9de5fa1`, `5b68e36`, `f24f0f9` | `test_controller.py::test_land_retries_after_transport_failure_without_mode_cache`; `test_control_ownership.py::test_takeover_during_landing_prevents_the_next_land_command`. Original pinned simulator cleanup/ownership gates passed. |
| F02 absent mode mapping | Copter mode policy is in the flight layer; the shared transport requires observed vehicle identity and does not invent a quadrotor when identity is absent. | `9de5fa1`, `799f1f8` | `test_shared_mavlink.py::test_mode_mapping_requires_observed_vehicle_identity`; controller failed-RX/no-mode-cache regression above explicitly verifies mapping is not queried. |
| F03 cleanup disarm retry | Retry only permitted ground disarm within the deadline; do not accept cached/queued pre-request disarm evidence or erase a pending arm obligation. | `9de5fa1`, `f24f0f9` | `test_controller.py::test_disarm_retries_after_failed_write_and_receive`, `test_disarm_requires_new_heartbeat_not_cached_or_queued_state`, `test_disarmed_heartbeat_during_pending_arm_preserves_cleanup`. |
| F04 takeoff datum | Pre-arm check uses measured floor datum + target gain + 0.05 m reserve against the unchanged ceiling; aligned-local and independent range guards remain. | `4017686`, `871d7f5`, `0c66bd1` | `test_controller.py::test_takeoff_reserves_overshoot_and_refuses_before_first_arm_write`, `test_prearm_rechecks_ground_envelope_after_mode_and_disarm_confirmation`; both final pinned floor-offset cases passed. Reserve is policy, not physical overshoot measurement. |
| F05 final power fallback | Every final FC observation, including direct fallback after runtime disappearance, passes the same fresh selected-FC disarmed guard immediately before the action. Original runtime disarm guard is retained. | `08020d1`, `813fce3`, `b192389` | `test_power.py::test_runtime_disappearing_before_final_power_check_cannot_authorize`, `test_final_action_requires_its_own_fresh_pi_uart`, `test_expired_fc_confirmation_cannot_request_shutdown`. No live shutdown was executed. |
| F06 summary failure | Resource shutdown precedes finalization; summary failures retain errors and attempt a minimal atomic failure manifest. An unusable filesystem still cannot promise a manifest. | `44602cf`, `487738b`, `c7b72d5`, `0988628` | `test_recording.py::test_summary_failure_writes_minimal_manifest`, `test_invalid_manifest_value_becomes_atomic_failure_manifest`, `test_lifecycle_attempts_all_cleanup_before_finalization`, `test_timestamp_parse_failure_cannot_publish_success_manifest`. |
| F07 interrupted mount hold | Distinguish attempted/issued commands, completed hold, detached PWM, interruption and error; preserve mount-open and normal pulse-rest behavior. No physical position feedback is invented. | `9afdded`, `44602cf` | `test_tag_servo_record.py::test_interrupted_mount_hold_reports_issued_without_completed_hold`, `test_stop_during_mount_open_detaches_without_reclosing`, `test_stop_during_active_pulse_returns_to_rest_without_counting`. GPIO output is mocked; physical actuation remains unauthorized. |
| F08 shutdown ownership | Quiesce endpoints and bound joins, retain errors and socket exclusion if any handler can still produce effects, then permit replacement only after later confirmed quiescence. | `799f1f8` | `test_vehicle_server.py::test_shutdown_timeout_retains_exclusion_until_handler_finishes` proves timeout blocks replacement and a later completed shutdown allows restart. |
| F09 stable servo lock | One `/run/ai-drone-locks/bcm12-servo.lock` namespace, boot-lifetime provisioning independent of restartable access-service directory, `O_NOFOLLOW`, owner/type/mode validation; no fallback lock path. | `8cd15e0`, `7907ff5` | `test_servo_lock.py::test_single_default_namespace_has_no_fallback`, `test_lock_survives_access_directory_restart`, `test_rejects_invalid_lock_inode`, `test_rejects_symlink_directory`; tag/standalone lifecycle tests retain exclusion through failed GPIO close. Live installation/restart remains deferred. |
| F10 settings policy | Deliberately retain silent optional CWD `drone.toml`, explicit-file failure and existing precedence; parse once and inject command-scoped immutable Settings without changing the environment. Antigravity's unconditional missing-optional-file warning was removed deliberately. No claim that CWD-relative defaults were a demonstrated wrong-host bug. | `405c49a` | `test_settings.py::test_defaults_need_no_file` asserts empty stderr and explicit missing-file failure; `test_file_defaults_and_environment_overrides`; `test_cli.py::test_dispatch_injects_one_settings_without_environment_mutation`. Exposing selected source is discretionary ('where useful'), not an unimplemented mandatory warning. |
| F11 network profile schema | Parse schema 1, profile collection, UUID keys, required fields and exact types before effects; malformed records fail instead of becoming empty policy. | `799f1f8`, `5fae80b` | `test_runtime.py::test_network_profiles_rejects_missing_or_invalid_schema`, `test_saved_network_policy_rejects_every_malformed_record`; network policy tests cover invalid identifiers and unsafe state before activation. |
| F12 recording names | New automatically named captures use UTC and preserve atomic collision handling; historical paths are not renamed. | `08020d1` | `test_recording.py::test_create_recording_paths_uses_utc_when_output_omitted`, `test_create_recording_paths_reserves_unique_directories_concurrently`. |
| F13 SSH builders | Consolidate equivalent SSH-prefix and quoted remote Python construction and callers; preserve config selection/argument semantics. | `8833802` | `test_cross_platform_scripts.py::test_deployment_and_connection_share_ssh_config_selection`, `test_config_sync_export_uses_normal_ssh_config_by_default`; `test_power.py::test_ssh_uses_existing_target_config_and_quoted_project`; transfer shell-character regressions. |
| F14 status/freshness policy | Shared strict parsing and monotonic arithmetic; restart health, maintenance disarm authorization, controller freshness and capture reporting remain distinct explicit policies. Recheck authorization at effect boundaries. | `5af3dee`, `813fce3`, `b192389` | `test_runtime_status.py::test_restart_health_does_not_authorize_armed_maintenance`, `test_effective_age_preserves_inclusive_boundary_and_original_publication`, `test_installer_receipt_policy_remains_distinct_from_published_snapshot_age`; power/control/capture stale and future timestamp regressions. |
| F15 range semantics | Shared finite positive wire-value/quality parsing, with explicit unspecified-bound reporting vs strict-control policy; full selected source/ID/orientation identity and invalid/replayed evidence revocation. | `5af3dee`, `babc8ae`, `2ced490`, `d9986d1` | `test_boundary_policy.py::test_range_unspecified_bounds_are_explicit_policy`, `test_range_rejects_invalid_wire_values`; controller/state/checker identity and invalid-sample tests; actual sensor-loss/orientation/discontinuity SITL cases passed. Native upstream ID is republished as configured FC instance ID, not a false fault injection. |
| F16 handoff return/response | Require a dictionary containing `handoff_requested is True`, return explicit integer success, reject malformed/false/nonboolean acknowledgements, and retain concrete runtime rejection errors. | `13312d2` | The audit reproduced false success for `False`, `None`, `{}` and `{'handoff_requested': False}` before the fix. The flight owner added acknowledgement regressions and passed 60 CLI/ownership tests. This validates request acknowledgement, not physical pilot ownership or RC readiness. |
| F17 recorder link load | Exact directional transport return counters, decoded delivery rates/bytes, age/backlog/overflow/discard metrics, sequence-loss estimate with explicit uncertainty, heartbeat/setpoint gaps. Streams were not reduced without evidence. | `1b89818`, `6df5903`, `57946ac`, `5194303` | `test_transport_metrics.py`, recording delivery regressions and two real host synthetic-camera/OpenCV/H264 tests passed. First actual full-load SITL run completed flight/capture/storage assertions but failed exact RX because pymavlink's idle empty-text sentinel was treated as unknown; `5194303` corrects only this accounting and its real-adapter regression passed. The unchanged isolated full-load rerun at `5194303` passed in 62.53 s: positive exact directional TCP byte counts, zero observed transport/queue errors, valid H264, real generated-tag detection and continued processing after video reserve stop. This is host synthetic-camera/CPU-codec evidence. Exact final Pi UART throughput/thermal/load qualification remains blocked by unreachable Pi, not fabricated from message counts or host TCP. Nominal 8N1 capacity remains 11,520 bytes/s per direction. |
| F18 rejected request lease | Deliberately preserve fail-closed behavior: rejection sends an error, disconnects the peer and releases its lease. No replacement may claim control while armed. The runtime does not synthesize continued control-owner heartbeats; the independent FC GCS-loss failsafe remains the recovery path. This is not a silent conversion to recoverable request errors. | `799f1f8` | `test_vehicle_server.py::test_rejected_owner_disconnects_and_cannot_be_replaced_while_armed` observes the original heartbeat, rejection/disconnect, no replacement write while armed, and successful claim only after fresh disarm. Original pinned hard GCS-loss test separately validates actual FC-side LAND/disarm. Physical aircraft failsafe qualification remains deferred. |
| F19 capture snapshot races | Locked mutations and coherent immutable copies for reporting/finalization; formatting, serialization and I/O remain outside the state lock. No reliance on interpreter GIL. | `487738b`, `44602cf` | `test_recording.py::test_capture_snapshot_freezes_each_concurrent_first_seen_key`, `test_capture_snapshot_is_coherent_and_owns_counter_copies`, cleanup-before-finalization regressions. |

The feasible F16 omission found by this bounded audit was closed in `13312d2`; F17's independent counter correction and unchanged full-load rerun also completed. Final Pi measurement and physical qualifications remain explicit blockers. The 30-second vertical-clearance and magnetic-compass yaw failures remain qualification failures and are not resolved by this register audit.
